# SiHEDAF Team Page Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a dedicated `/tim-kami` route that matches the supplied team-page reference and is reachable from shared navigation.

**Architecture:** The route composes the existing shared header/footer around a new `TeamSection`. A reusable `TeamCard` renders each blank portrait placeholder, while `HeaderSection` receives an optional active-page prop and shared navigation moves to `next/link`.

**Tech Stack:** Next.js 16.2 App Router, React 19 Server Components, Tailwind CSS 4, TypeScript, Node test runner

---

### Task 1: Team-page route contract

**Files:**
- Create: `src/app/TeamPage.test.mjs`
- Modify: `src/app/LandingPage.test.mjs`

- [x] **Step 1: Describe the route and component contract**

Require `src/app/tim-kami/page.tsx`, `src/components/sections/TeamSection.tsx`, and `src/components/ui/TeamCard.tsx`. Verify PascalCase component filenames, heading copy, four role labels, four card entries, `next/link`, active navigation, and no image element inside the blank card component.

- [x] **Step 2: Update the landing navigation contract**

Replace the old `#tim-kami` anchor expectation with the `/tim-kami` route while retaining the existing home-section anchor checks.

- [x] **Step 3: Verify RED**

Run: `rtk npm test`

Expected: existing tests pass except the new team-page contract, which fails because the route and components are missing.

### Task 2: Shared route-aware navigation

**Files:**
- Modify: `src/components/ui/BrandLogo.tsx`
- Modify: `src/components/sections/HeaderSection.tsx`
- Modify: `src/components/sections/FooterSection.tsx`

- [x] **Step 1: Use optimized internal links**

Import `Link` from `next/link`; point the brand to `/`, home sections to `/#tentang`, `/#fitur`, and `/#cara-kerja`, and team links to `/tim-kami`.

- [x] **Step 2: Add active team styling**

Give `HeaderSection` an optional `activePage?: "home" | "team"` prop and render the “Tim Kami” item in primary blue when `activePage` is `team`.

- [x] **Step 3: Match the reference header width**

Use a responsive width near 86.5% with the existing 1200px cap so the floating header aligns with the team grid in the supplied desktop reference.

### Task 3: Team cards and route composition

**Files:**
- Create: `src/components/ui/TeamCard.tsx`
- Create: `src/components/sections/TeamSection.tsx`
- Create: `src/app/tim-kami/page.tsx`

- [x] **Step 1: Build the blank portrait card**

Render a portrait-aspect gray gradient card with a white role pill and bottom-aligned white name and LinkedIn label. Do not render `next/image` or an `img` element.

- [x] **Step 2: Build the team section**

Render the centered “Tim Pengembang dibalik SiHEDAF” heading and four cards for UI/UX Designer, Frontend Developer, Backend Developer, and AI Developer.

- [x] **Step 3: Compose the static route**

Render `HeaderSection activePage="team"`, `TeamSection`, and `FooterSection` in a viewport-height flex column at `src/app/tim-kami/page.tsx`.

- [x] **Step 4: Verify GREEN**

Run: `rtk npm test`

Expected: all design-system, landing-page, and team-page tests pass.

### Task 4: Framework and render verification

**Files:**
- Modify only team or shared navigation files if verification exposes discrepancies

- [x] **Step 1: Run lint and build**

Run: `rtk npm run lint` and `rtk npm run build`.

Expected: both succeed and the route table contains static `/` and `/tim-kami` entries.

- [x] **Step 2: Verify production navigation and markup**

Confirm `/` contains a link to `/tim-kami`, `/tim-kami` returns HTTP 200, and its HTML contains the four roles and four blank gradient cards.

- [x] **Step 3: Audit the final diff**

Confirm all new non-framework files are PascalCase, no `use client` boundary was introduced, and user-provided image assets remain untouched.
